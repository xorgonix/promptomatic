```mermaid
mindmap
    direction TB
        root((Experience))
            hallucinate(Hallucinate sometimes)
                reason[For some reason]
            honorific(Receives plain honorific)
                cringe[Kinda cringe]

```